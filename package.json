{
  "name": "ziscar",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run start:debug",
    "start": "npm run frontend & npm run backend"
  }
}
