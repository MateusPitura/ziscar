import type { ReactNode } from 'react';
import EditCustomerContainer from './EditCustomerContainer';

export default function EditCustomerPage(): ReactNode {
  return (
    <EditCustomerContainer/>
  );
}